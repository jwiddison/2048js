import React from "react";
import { Background, Grid } from "./BoardStyledComponents";

const Board = props => {
  return (
    <Background>
      <Grid />
    </Background>
  );
};

export default Board;
